# @upsideos/sui-rwa-artifacts

Placeholder. The manifest schema already accepts `sui-move`.
The Sui adapter in `@upsideos/contract-artifacts-cli` throws until
Move toolchain verification is wired in.
