# @upsideos/evm-rwa-artifacts

The EVM artifacts package is published from the repository root so that a plain
`npm install @upsideos/evm-rwa-artifacts` resolves the packed releases under
[`releases/`](../../releases) with no workspace protocol.

See the [root README](../../README.md).
