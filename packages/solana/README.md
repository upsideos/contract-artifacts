# @upsideos/solana-rwa-artifacts

Placeholder. The manifest schema already accepts `solana-verifiable-build`.
The Solana adapter in `@upsideos/contract-artifacts-cli` throws until
docker-based verifiable builds are wired in.

The generated client remains `@upsideos/solana-rwa`.
